# Auth MVP — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar sign-up da agência, sign-in do cliente, callback de invite e endpoint para convidar clientes via email.

**Architecture:** Supabase Auth com `app_metadata.role` (`agency` | `client`). Páginas em Next.js App Router. Service role key usada nas API routes para operações admin (setar role, enviar invite).

**Tech Stack:** Next.js 16 App Router, `@supabase/ssr`, `@supabase/supabase-js` admin, Tailwind CSS.

## Global Constraints

- Supabase client no browser: `@/lib/supabase` (singleton com anon key)
- Supabase admin (service role): criar inline nas API routes com `createClient(url, SERVICE_ROLE_KEY)`
- Estilos: Tailwind, seguir padrão do `/sign-in` existente (card branco, bg `#f5f5f5`, azul `#1A3A66`, roxo `#8b5cf6`)
- Sem bibliotecas de UI novas — só Tailwind puro
- Todos os redirects após auth usam `router.push` + `router.refresh()`

---

### Task 1: Middleware + callback route

**Files:**
- Modify: `src/middleware.ts`
- Create: `src/app/auth/callback/route.ts`

**Interfaces:**
- Produz: rota `/auth/callback` que troca `code` por sessão e redireciona por role

- [ ] **Step 1: Atualizar PUBLIC_PATHS no middleware**

Em `src/middleware.ts`, linha 4, trocar:
```ts
const PUBLIC_PATHS = ["/sign-in", "/client/sign-in"];
```
por:
```ts
const PUBLIC_PATHS = ["/sign-in", "/sign-up", "/client/sign-in", "/auth/callback"];
```

- [ ] **Step 2: Criar route handler do callback**

Criar `src/app/auth/callback/route.ts`:
```ts
import { createServerClient } from "@supabase/ssr";
import { cookies } from "next/headers";
import { NextRequest, NextResponse } from "next/server";

export async function GET(request: NextRequest) {
  const { searchParams, origin } = new URL(request.url);
  const code = searchParams.get("code");

  if (!code) return NextResponse.redirect(`${origin}/sign-in`);

  const cookieStore = await cookies();
  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll: () => cookieStore.getAll(),
        setAll: (toSet) => toSet.forEach(({ name, value, options }) => cookieStore.set(name, value, options)),
      },
    }
  );

  const { error } = await supabase.auth.exchangeCodeForSession(code);
  if (error) return NextResponse.redirect(`${origin}/sign-in?error=callback`);

  const { data: { user } } = await supabase.auth.getUser();
  const role = (user?.app_metadata as { role?: string })?.role;

  if (role === "client") return NextResponse.redirect(`${origin}/client/dashboard`);
  return NextResponse.redirect(`${origin}/`);
}
```

- [ ] **Step 3: Verificar build**

```bash
cd packages/web && npx tsc --noEmit
```
Esperado: sem erros.

- [ ] **Step 4: Commit**

```bash
git add packages/web/src/middleware.ts packages/web/src/app/auth/callback/route.ts
git commit -m "feat: auth callback route e public paths atualizados"
```

---

### Task 2: API route — set-agency-role

**Files:**
- Create: `src/app/api/auth/set-agency-role/route.ts`

**Interfaces:**
- Consome: `{ userId: string }` no body
- Produz: `{ ok: true }` ou `{ error: string }`

- [ ] **Step 1: Criar endpoint**

Criar `src/app/api/auth/set-agency-role/route.ts`:
```ts
import { NextRequest, NextResponse } from "next/server";
import { createClient } from "@supabase/supabase-js";

export async function POST(req: NextRequest) {
  const { userId } = await req.json().catch(() => ({}));
  if (!userId) return NextResponse.json({ error: "userId obrigatório" }, { status: 400 });

  const admin = createClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
    { auth: { persistSession: false } }
  );

  const { error } = await admin.auth.admin.updateUserById(userId, {
    app_metadata: { role: "agency" },
  });

  if (error) return NextResponse.json({ error: error.message }, { status: 500 });
  return NextResponse.json({ ok: true });
}
```

- [ ] **Step 2: Testar via curl (com servidor rodando)**

```bash
curl -s -X POST http://localhost:3000/api/auth/set-agency-role \
  -H "Content-Type: application/json" \
  -d '{"userId":"test"}' | cat
```
Esperado: `{"error":"..."}` (erro do Supabase pois userId inválido, mas a rota responde).

- [ ] **Step 3: Commit**

```bash
git add packages/web/src/app/api/auth/set-agency-role/route.ts
git commit -m "feat: endpoint set-agency-role para signup da agência"
```

---

### Task 3: API route — invite-client

**Files:**
- Create: `src/app/api/auth/invite-client/route.ts`

**Interfaces:**
- Consome: `{ clientId: string, email: string, name: string }`
- Produz: `{ ok: true }` ou `{ error: string }`

- [ ] **Step 1: Criar endpoint**

Criar `src/app/api/auth/invite-client/route.ts`:
```ts
import { NextRequest, NextResponse } from "next/server";
import { createServerClient } from "@supabase/ssr";
import { createClient } from "@supabase/supabase-js";
import { cookies } from "next/headers";

export async function POST(req: NextRequest) {
  // Verificar que chamador é agency
  const cookieStore = await cookies();
  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll: () => cookieStore.getAll(),
        setAll: (toSet) => toSet.forEach(({ name, value, options }) => cookieStore.set(name, value, options)),
      },
    }
  );
  const { data: { user } } = await supabase.auth.getUser();
  const role = (user?.app_metadata as { role?: string })?.role;
  if (role !== "agency") return NextResponse.json({ error: "Não autorizado" }, { status: 403 });

  const { clientId, email, name } = await req.json().catch(() => ({}));
  if (!clientId || !email) {
    return NextResponse.json({ error: "clientId e email são obrigatórios" }, { status: 400 });
  }

  const admin = createClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
    { auth: { persistSession: false } }
  );

  const { error } = await admin.auth.admin.inviteUserByEmail(email, {
    data: { role: "client", clientId, name: name ?? "" },
    redirectTo: `${process.env.NEXT_PUBLIC_APP_URL ?? "http://localhost:3000"}/auth/callback`,
  });

  if (error) return NextResponse.json({ error: error.message }, { status: 500 });
  return NextResponse.json({ ok: true });
}
```

- [ ] **Step 2: Adicionar NEXT_PUBLIC_APP_URL ao .env**

No `.env` raiz e em `packages/web/.env.local`, adicionar:
```
NEXT_PUBLIC_APP_URL=http://localhost:3000
```
(Em produção na Vercel, setar para o domínio real.)

- [ ] **Step 3: Commit**

```bash
git add packages/web/src/app/api/auth/invite-client/route.ts
git commit -m "feat: endpoint invite-client com Supabase admin invite"
```

---

### Task 4: Página /sign-up (agência)

**Files:**
- Create: `src/app/sign-up/page.tsx`
- Modify: `src/app/sign-in/[[...sign-in]]/page.tsx` (adicionar link)

**Interfaces:**
- Consome: `POST /api/auth/set-agency-role`
- Consome: `supabase.auth.signUp`

- [ ] **Step 1: Criar página de sign-up**

Criar `src/app/sign-up/page.tsx`:
```tsx
"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import Link from "next/link";
import { supabase } from "@/lib/supabase";

export default function SignUpPage() {
  const router = useRouter();
  const [name, setName] = useState("");
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [confirm, setConfirm] = useState("");
  const [error, setError] = useState("");
  const [loading, setLoading] = useState(false);

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault();
    if (password !== confirm) { setError("As senhas não coincidem."); return; }
    if (password.length < 6) { setError("A senha deve ter pelo menos 6 caracteres."); return; }
    setLoading(true);
    setError("");

    const { data, error: signUpError } = await supabase.auth.signUp({
      email,
      password,
      options: { data: { name } },
    });

    if (signUpError || !data.user) {
      setError(signUpError?.message ?? "Erro ao criar conta.");
      setLoading(false);
      return;
    }

    // Setar role agency via service role
    const res = await fetch("/api/auth/set-agency-role", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ userId: data.user.id }),
    });

    if (!res.ok) {
      setError("Conta criada, mas erro ao configurar permissões. Tente fazer login.");
      setLoading(false);
      return;
    }

    router.push("/");
    router.refresh();
  }

  return (
    <div className="flex min-h-screen items-center justify-center bg-[#f5f5f5]">
      <form onSubmit={handleSubmit} className="bg-white rounded-2xl shadow p-8 w-full max-w-sm space-y-4">
        <h1 className="text-xl font-bold text-[#1A3A66]">Clique Boost</h1>
        <p className="text-sm text-gray-500">Criar conta da agência</p>

        <div className="space-y-2">
          <label className="text-sm font-medium text-gray-700">Nome completo</label>
          <input
            type="text"
            required
            value={name}
            onChange={(e) => setName(e.target.value)}
            className="w-full border rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-[#1A3A66]"
          />
        </div>

        <div className="space-y-2">
          <label className="text-sm font-medium text-gray-700">Email</label>
          <input
            type="email"
            required
            value={email}
            onChange={(e) => setEmail(e.target.value)}
            className="w-full border rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-[#1A3A66]"
          />
        </div>

        <div className="space-y-2">
          <label className="text-sm font-medium text-gray-700">Senha</label>
          <input
            type="password"
            required
            value={password}
            onChange={(e) => setPassword(e.target.value)}
            className="w-full border rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-[#1A3A66]"
          />
        </div>

        <div className="space-y-2">
          <label className="text-sm font-medium text-gray-700">Confirmar senha</label>
          <input
            type="password"
            required
            value={confirm}
            onChange={(e) => setConfirm(e.target.value)}
            className="w-full border rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-[#1A3A66]"
          />
        </div>

        {error && <p className="text-sm text-red-500">{error}</p>}

        <button
          type="submit"
          disabled={loading}
          className="w-full bg-[#1A3A66] text-white rounded-lg py-2 text-sm font-semibold disabled:opacity-50"
        >
          {loading ? "Criando conta..." : "Criar conta"}
        </button>

        <p className="text-center text-xs text-gray-500">
          Já tem conta?{" "}
          <Link href="/sign-in" className="text-[#1A3A66] font-medium hover:underline">
            Entrar
          </Link>
        </p>
      </form>
    </div>
  );
}
```

- [ ] **Step 2: Adicionar link "Criar conta" no /sign-in**

Em `src/app/sign-in/[[...sign-in]]/page.tsx`, após o `<button>` de submit, adicionar:
```tsx
import Link from "next/link";

// após o botão Entrar:
<p className="text-center text-xs text-gray-500">
  Não tem conta?{" "}
  <Link href="/sign-up" className="text-[#1A3A66] font-medium hover:underline">
    Criar conta
  </Link>
</p>
```

- [ ] **Step 3: Verificar build**

```bash
cd packages/web && npx tsc --noEmit
```
Esperado: sem erros.

- [ ] **Step 4: Testar manualmente**
  - Acessar `http://localhost:3000/sign-up`
  - Preencher nome, email, senha e confirmar
  - Verificar redirecionamento para `/`
  - Verificar no Supabase Dashboard → Authentication → Users que o usuário foi criado com `app_metadata.role = "agency"`

- [ ] **Step 5: Commit**

```bash
git add packages/web/src/app/sign-up/page.tsx packages/web/src/app/sign-in/[[...sign-in]]/page.tsx
git commit -m "feat: página sign-up da agência com set de role automático"
```

---

### Task 5: Página /client/sign-in

**Files:**
- Create: `src/app/client/sign-in/page.tsx`

- [ ] **Step 1: Criar página**

Criar `src/app/client/sign-in/page.tsx`:
```tsx
"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import { supabase } from "@/lib/supabase";

export default function ClientSignInPage() {
  const router = useRouter();
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [error, setError] = useState("");
  const [loading, setLoading] = useState(false);

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault();
    setLoading(true);
    setError("");

    const { error: authError } = await supabase.auth.signInWithPassword({ email, password });
    if (authError) {
      setError("Email ou senha incorretos.");
      setLoading(false);
      return;
    }

    router.push("/client/dashboard");
    router.refresh();
  }

  return (
    <div className="flex min-h-screen items-center justify-center bg-[#f5f5f5]">
      <form onSubmit={handleSubmit} className="bg-white rounded-2xl shadow p-8 w-full max-w-sm space-y-4">
        <div className="flex items-center gap-2 mb-1">
          <div className="w-7 h-7 rounded-lg bg-[#8b5cf6] flex items-center justify-center">
            <span className="text-white text-xs font-bold">CB</span>
          </div>
          <h1 className="text-xl font-bold text-[#1A3A66]">Clique Boost</h1>
        </div>
        <p className="text-sm text-gray-500">Portal do Cliente</p>

        <div className="space-y-2">
          <label className="text-sm font-medium text-gray-700">Email</label>
          <input
            type="email"
            required
            value={email}
            onChange={(e) => setEmail(e.target.value)}
            className="w-full border rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-[#8b5cf6]"
          />
        </div>

        <div className="space-y-2">
          <label className="text-sm font-medium text-gray-700">Senha</label>
          <input
            type="password"
            required
            value={password}
            onChange={(e) => setPassword(e.target.value)}
            className="w-full border rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-[#8b5cf6]"
          />
        </div>

        {error && <p className="text-sm text-red-500">{error}</p>}

        <button
          type="submit"
          disabled={loading}
          className="w-full bg-[#8b5cf6] text-white rounded-lg py-2 text-sm font-semibold hover:bg-[#7c3aed] transition-colors disabled:opacity-50"
        >
          {loading ? "Entrando..." : "Entrar"}
        </button>

        <p className="text-center text-xs text-gray-400">
          Acesse com o email e senha enviados pela sua agência.
        </p>
      </form>
    </div>
  );
}
```

- [ ] **Step 2: Verificar build**

```bash
cd packages/web && npx tsc --noEmit
```
Esperado: sem erros.

- [ ] **Step 3: Testar manualmente**
  - Acessar `http://localhost:3000/client/sign-in`
  - Tentar login com credenciais inválidas → ver mensagem de erro
  - Login com credenciais válidas de cliente → redirecionar para `/client/dashboard`

- [ ] **Step 4: Commit**

```bash
git add packages/web/src/app/client/sign-in/page.tsx
git commit -m "feat: página sign-in do portal do cliente"
```

---

### Task 6: Adicionar variável NEXT_PUBLIC_APP_URL

**Files:**
- Modify: `.env` (raiz)
- Modify: `packages/web/.env.local`

- [ ] **Step 1: Adicionar ao .env raiz**

```bash
echo "" >> .env
echo "NEXT_PUBLIC_APP_URL=http://localhost:3000" >> .env
```

- [ ] **Step 2: Adicionar ao .env.local do web**

```bash
echo "" >> packages/web/.env.local
echo "NEXT_PUBLIC_APP_URL=http://localhost:3000" >> packages/web/.env.local
```

- [ ] **Step 3: Commit**

```bash
git add .env
git commit -m "chore: adiciona NEXT_PUBLIC_APP_URL ao env"
```
